# 🛠️ คู่มือซ่อมเว็บเอง (ฉบับเฮีย ไม่ต้องพึ่งใคร)

> หลักคิดเดียว: **เว็บ = 2 ส่วน**
> 1. **หน้าเว็บ** (index.html) — สิ่งที่เห็น/กด
> 2. **API** (api/chat.js) — สมองที่ตอบกลับ
>
> เว็บพัง = หนึ่งในสองส่วนนี้พัง → หาให้เจอว่าส่วนไหน แล้วแก้ส่วนนั้น

---

## 1️⃣ อาวุธลับ: ปุ่ม 🛠️ ในแอป (เริ่มที่นี่ก่อนเสมอ)

กดปุ่ม **🛠️** บนหัวแอป → จะโชว์ผลตรวจ 3 อย่าง:
- **KEY บนเซิร์ฟเวอร์** มีไหม?
- **Key ใช้ได้ไหม** (เงินหมด/ถูกรีโวค)
- **โมเดล** มีในบัญชีไหม?

ผลออกแดงตรงไหน → แก้ตามข้อความที่มันบอก → เสร็จ

---

## 2️⃣ ตารางอาการ → สาเหตุ → วิธีแก้

| อาการ | สาเหตุ | วิธีแก้ (ทำเองได้หมด) |
|---|---|---|
| เว็บเปิดไม่ขึ้น (ขาว/404) | Deploy พัง หรือไฟล์ผิดที่ | Vercel → Project → Deployments → กดอันล่าสุด → ดู Build Logs ว่า error บรรทัดไหน |
| กดส่งแล้วไม่เด้ง | ดู Console: F12 → Tab Console | ถ้ามี error สีแดง → อ่านข้อความ → ส่งมาให้กูก็ได้ 55 |
| ส่งแล้วขึ้น "ยังไม่ได้ตั้งค่า OPENROUTER_API_KEY" | ยังไม่ได้ตั้ง key | Vercel → Settings → Environment Variables → เพิ่ม `OPENROUTER_API_KEY` → Redeploy |
| ส่งแล้ว error 401 / 429 | Key หมดอายุ / **เงินหมด** | ไป openrouter.ai/activity ดูยอด → เติมเงิน หรือเปลี่ยน key ใหม่ |
| ตอบช้า / ขึ้น "45 วิ" | โมเดลคิดนาน (DeepSeek บางทีคิดเยอะ) | รออีกที หรือเปลี่ยนโมเดลถูก/เร็วกว่าในข้อ 4 |
| เงียบเป็นบางคน (โหมดทั้งคู่) หรือขึ้น "Prompt tokens limit exceeded" | **คีย์ยังเป็นฟรีเทียร์** — ขีดจำกัด prompt ~2,771 tokens | 1) เติมเงินที่ openrouter.ai/settings/credits (หายขาด) 2) หรือระบบ fallback ให้อัตโนมัติแล้ว (DeepSeek เงียบ → สลับโมเดลฟรี ling-3.0-flash) |
| อยากลองโมเดลอื่น/โมเดลแพง | แค่คิด — กล่อง "🧠 โมเดล" บนหน้าเว็บมีให้เลือก 15 ตัว | เลือกได้เลย: ตัวฟรีใช้ได้ทันที ตัวแพง (GPT-5/Claude/Gemini Pro) ต้องเครดิต > $0 — ตอนนี้เครดิต $0 ระบบจะ fallback ฟรีให้พร้อมบอกสาเหตุ |
| ตอบไม่เพราะ / พูดไม่เป็นตัวเอง | บุคลิกหาย | ตั้ง env `PERSONA` (ดูข้อ 4) |
| อยากลองโมเดลแพง / โมเดลอื่น | คิดว่าต้องแก้โค้ด | ไม่ต้อง! กล่อง **"🧠 โมเดล"** บนหน้าเว็บเลือกได้ 15 ตัว — ตัวฟรีใช้ได้เลย ตัวแพง (GPT-5/Claude/Gemini Pro) ต้องเครดิต > $0 ไม่งั้นระบบ fallback ฟรีให้พร้อมบอกสาเหตุ |
| อยากเช็คว่า API ทำงานไหม | — | เปิดเบราว์เซอร์ → เข้า `https://เว็บเฮีย/api/health` ตรง ๆ เลย ถ้าได้ JSON = ระบบยังมีชีวิต ✅ |

---

## 3️⃣ วิธีดู Log (รู้ทันทีว่าพังตรงไหน)

**Vercel:** Dashboard → โปรเจกต์ → Tab **Logs** (หรือ Deployments → เลือกรอบล่าสุด → Logs)
- เห็นคำว่า `OpenRouter error: 401` → ปัญหา key
- เห็น error อื่น → อ่านข้อความมัน บอกอยู่แล้วว่าบรรทัดไหน

---

## 4️⃣ ตั้งค่า/เปลี่ยนของในแอป (ไม่ต้องแตะโค้ดเลย)

ไปที่ **Vercel → Settings → Environment Variables** แล้วเพิ่ม:

| ชื่อ | ค่า | ใช้ทำอะไร |
|---|---|---|
| `OPENROUTER_API_KEY` | `sk-or-v1-...` | **จำเป็นที่สุด** ไม่มี = แชทไม่ทำงาน |
| `OPENROUTER_MODEL` | `deepseek/deepseek-v4-pro` | โมเดลหลัก (ค่าเริ่มต้น) |
| `OPENROUTER_MODEL` | `inclusionai/ling-3.0-flash` | โมเดลถูกสุด เร็ว ใช้ยาว ๆ |
| `PERSONA` | ข้อความบุคลิกสลี่ | เปลี่ยนนิสัยสลี่ (เขียนเป็นไทยได้เลย) |
| `TEACHER_PERSONA` | ข้อความบุคลิกครู | เปลี่ยนวิธีสอนของครู |
| `OPENROUTER_FALLBACK` | `inclusionai/ling-3.0-flash` | โมเดลสำรอง (ฟรี) ใช้เมื่อโมเดลหลักเงียบ/พลาด |
| `OPENROUTER_MODEL` | `deepseek/deepseek-v4-pro` | โมเดลหลัก (ตัวที่หน้าเว็บเลือก "อัตโนมัติ") |

⚠️ **แก้ env แล้วต้อง Redeploy** (Vercel → Deployments → ⋯ → Redeploy) ไม่งั้นไม่生效!

## 6️⃣ เครื่องมือครู — หมายเหตุ
- 💻 **รันโค้ด** ใช้ `execFile` บนเครื่อง (Python/JS, timeout 8 วิ) — **Vercel serverless รันไม่ได้** → จะ fallback ไป Piston (ปัจจุบัน whitelist) → อยากให้รันโค้ดได้จริงต้อง self-host (VPS/คอนเทนเนอร์/Cloud Run)
- 🔍 **ค้นหา** = Wikipedia ไทย + DuckDuckGo (ฟรี ไม่ต้อง key) · 🗄️ **ฐานข้อมูล** = SQL จำลอง (localStorage ในเบราว์เซอร์) · 📖 **อ่านโค้ด** = whitelist 5 ไฟล์ (`index|chat|server|readme|fix` ผ่าน `GET /api/file?name=...`)
- ทุกข้อความตอบมีปุ่ม **📋 คัดลอก** ที่มุมขวาบนของกล่อง (กดแล้วคัดลอกข้อความต้นฉบับ)
- ระบบ AI ตอนนี้: ลอง 3 โมเดล (หลัก → fallback → openrouter/auto) × retry 2 รอบ ก่อนยอมแพ้

## 7️⃣ โมเดลฟรี OpenRouter หมดเดือน ก.พ. → ใช้ Gemini/Groq ฟรีแทน
- **ข่าว:** OpenRouter อาจดึงโมเดล `:free` ออกเดือน ก.พ. → ถ้าแชทเงียบ/มี note "โมเดลหลักใช้ไม่ได้" บ่อย ๆ แปลว่า free หมดแล้ว
- **วิธีรอด (ฟรี 100%):**
  - **Groq (ง่ายสุด — แนะนำ):** https://console.groq.com/keys → Create API Key → key ขึ้นต้น `gsk_` → ใส่ env `GROQ_API_KEY` → Redeploy
  - **Gemini:** https://aistudio.google.com/apikey → Create API key → **ต้องได้ key ขึ้นต้น `AIza...`** (ถ้าได้ `AQ.`/`ya29.` = OAuth token ชั่วคราว หมดอายุ ~1 ชม. ใช้ไม่ได้!) → ใส่ env `GOOGLE_API_KEY`
- ระบบจะลองฟรีสำรองก่อน openrouter/auto — เช็คได้จากปุ่ม 🔧 วินิจฉัย (แถว Gemini/Groq)
- เปลี่ยนโมเดล: env `GEMINI_MODEL` / `GROQ_MODEL`
- ทางเลือกจ่ายจริงถูกมาก: `ling-3.0-flash` ~$0.02/ล้าน tokens — $1 ใช้ได้นานมาก

---

## 5️⃣ วิธีแก้โค้ดเองเบื้องต้น

1. แก้ไฟล์ในเครื่อง (`public/index.html` = หน้าตา, `api/chat.js` = สมอง)
2. `git add . && git commit -m "..." && git push` → Vercel deploy อัตโนมัติ
3. รอ ~1 นาที → เปิดเว็บใหม่ (กด Ctrl+F5 / ล้างแคช)

**ทดสอบที่เครื่องก่อน deploy:**
```bash
cd โฟลเดอร์โปรเจกต์
OPENROUTER_API_KEY=sk-or-v1-xxx node server.js
# เปิด http://localhost:3000
```

---

## 6️⃣ ถ้ายังงง

กฎเหล็ก: **อย่าเดา** — เอาข้อมูล 3 อย่างนี้มาให้กู:
1. หน้าจอ error / ข้อความที่เห็น (ถ่ายรูปก็ได้)
2. Console error (F12)
3. Vercel Logs

กูบอกให้ได้ทันทีว่าพังตรงไหน + สอนวิธีแก้ — แล้วเฮียก็จะค่อย ๆ ซ่อมเองเป็นเอง 55 💜
