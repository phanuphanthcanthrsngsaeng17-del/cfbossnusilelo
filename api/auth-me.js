import crypto from 'node:crypto';
function parse(req){return Object.fromEntries((req.headers.cookie||'').split(';').map(x=>x.trim().split('=' )).filter(x=>x.length===2).map(([k,v])=>[k,decodeURIComponent(v)]))}
function verify(token){const secret=process.env.JWT_SECRET;if(!secret||!token)return null;const [h,p,s]=String(token).split('.');if(!h||!p||!s)return null;const expected=crypto.createHmac('sha256',secret).update(`${h}.${p}`).digest('base64url');if(s.length!==expected.length||!crypto.timingSafeEqual(Buffer.from(s),Buffer.from(expected)))return null;const data=JSON.parse(Buffer.from(p,'base64url').toString());if(!data.exp||data.exp<Math.floor(Date.now()/1000))return null;return data}
export default function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method Not Allowed'});const user=verify(parse(req).cf_session);res.setHeader('Cache-Control','no-store');return res.status(200).json({authenticated:Boolean(user),user:user?{name:user.name,email:user.email,picture:user.picture,provider:user.provider}:null})}
